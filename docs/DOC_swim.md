protocol_swim.c
│
├── ============================================================
├──  بخش ۱: هدرها و تعاریف (Headers & Defines)
├── ============================================================
│
├── ============================================================
├──  بخش ۲: پین‌ها و تایمینگ‌ها (Pins & Timing)
├── ============================================================
│
├── ============================================================
├──  بخش ۳: ⭐ رجیسترهای SWIM برای همه خانواده‌ها (NEW)
├── ============================================================
│   ├── swim_regs_t (ساختار)
│   ├── g_swim_regs[] (دیتابیس)
│   ├── swim_detect_family()
│   ├── swim_get_regs()
│   └── swim_get_page_size()
│
├── ============================================================
├──  بخش ۴: توابع پایه GPIO (Low-Level)
├── ============================================================
│   ├── swim_ccount()
│   ├── swim_delay_cycles()
│   └── swim_entry_gpio()
│
├── ============================================================
├──  بخش ۵: Entry + SYNC
├── ============================================================
│   └── swim_entry_and_sync()
│
├── ============================================================
├──  بخش ۶: ارسال/دریافت بیت (Bit-Level)
├── ============================================================
│   ├── swim_send_byte()
│   └── swim_recv_byte()
│
├── ============================================================
├──  بخش ۷: دستورات SWIM (Commands)
├── ============================================================
│   ├── swim_srst()
│   ├── swim_rotf()
│   └── swim_wotf()
│
├── ============================================================
├──  بخش ۸: ⭐ Unlock و Write Flash/EEPROM (NEW)
├── ============================================================
│   ├── swim_flash_unlock()    ← جدید
│   ├── swim_eeprom_unlock()   ← جدید
│   └── swim_flash_write()     ← جدید (جایگزین swim_write_mem)
│
├── ============================================================
├──  بخش ۹: توابع سطح بالا (API)
├── ============================================================
│   ├── swim_init()
│   ├── swim_connect()
│   ├── swim_deinit()
│   ├── swim_detect()
│   ├── swim_read_mem()
│   ├── swim_write_mem()       ← استفاده از swim_flash_write
│   └── swim_erase()
│
└── ============================================================
    بخش ۱۰: تعریف پروتکل (Protocol Definition)
    ============================================================
    └── g_swim_protocol